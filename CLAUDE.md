# 文本格式批量转换器

## 项目概述

编写一个文本文件批量格式检测和转换软件。

## 技术选型

| 组件 | 选择 | 理由 |
|------|------|------|
| 语言 | Python 3.x | Windows 支持良好，生态丰富，编码处理库成熟 |
| GUI | Tkinter | Python 标准库，无需额外安装，轻量 |
| 编码检测 | chardet | 业界标准，支持中文编码（GBK、GB18030、UTF-8 等） |
| 并发 | concurrent.futures.ThreadPoolExecutor | 标准库，线程池管理简单可靠 |
| 程序后缀 | .pyw | 双击直接弹出 GUI 窗口，无控制台黑框 |

## 软件功能需求

1. **简单 GUI 界面**：用户指定处理路径，选择/管理文件后缀。
2. **可修改的文件后缀清单**：默认为 `.txt`、`.md`、`.py`，支持添加/删除。
3. **转换流程**：
   - 检索指定文件夹下所有匹配文件（含子目录），生成文件清单
   - 逐个处理：检测编码 -> 读取内容 -> 写 UTF-8 (no BOM) -> 验证读取
   - 验证失败时用缓存内容回滚原始文件
4. **完成提示**：弹出对话框显示处理数量、成功/失败/跳过情况
5. **暂停/终止**：转换过程中用户可暂停、恢复或停止
6. **并发策略**：文件数 >= 10 时多线程并行（最多 4 worker），< 10 时单线程
7. **线程隔离**：GUI 线程与工作线程独立，通过 `queue.Queue` 通信，GUI 通过 `root.after()` 轮询更新

## 架构设计

```
TextEncodingConverter.pyw
├── 依赖检查 (ensure_chardet)
│   └── 未安装时弹出对话框引导 pip install
├── 编码检测与转换
│   ├── detect_encoding() — chardet 检测
│   ├── try_decode() — 多编码尝试解码
│   ├── is_garbled() — 启发式乱码判断
│   └── convert_single_file() — 单文件转换（含回滚）
├── 文件扫描 (scan_files)
│   └── os.walk 递归扫描
├── 控制器 (ConverterController)
│   ├── 线程管理（start/pause/resume/stop）
│   ├── threading.Event 控制暂停/停止
│   └── 并发策略（<10 串行，>=10 线程池）
└── GUI (MainWindow)
    ├── 路径选择 + 后缀管理
    ├── 日志输出区
    ├── 进度条
    └── 控制按钮（转换/暂停/恢复/停止/退出）
```

## 异常处理

| 异常类型 | 处理方式 |
|----------|----------|
| chardet 无法识别编码 | 遍历候选编码（GBK/GB18030/Big5/latin-1），最后用 latin-1 兜底 |
| 文件被占用/权限不足 | 记录错误，跳过该文件 |
| 写入后验证失败 | 用缓存内容回滚原文件 |
| 依赖缺失 | 弹出对话框引导安装，不直接报错 |

## 退出机制

- 点击退出时如正在转换，弹出确认对话框
- 确认后触发 stop_event，等效于停止操作
- 工作线程为 daemon 线程，窗口关闭后自动终止

## 文件结构

```
文本格式自动转换器/
├── TextEncodingConverter.pyw  # 主程序（GUI + 逻辑）
├── CLAUDE.md         # 项目文档
└── Tests/            # 测试文件目录
    ├── 文本-ANSI.txt
    ├── 文本-GB18030.txt
    ├── 文本-UTF-16BE.txt
    ├── 文本-UTF-16LE.txt
    ├── 文本-UTF-8-BOM.txt
    └── 文本-UTF-8.txt
```
